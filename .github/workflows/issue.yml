name: get issue data
on: issues
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: get context
        run: echo " ${{ toJson(github) }}"
