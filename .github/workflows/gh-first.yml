name: Github first workflow
on: workflow_dispatch
jobs:
  fisrt-job:
    runs-on: ubuntu-latest
    steps:
      - name: print Hello world
        run: echo "Hello world"
  second-job: 
     runs-on: ubuntu-latest
     steps:
       - name: context
         run: echo "${{ toJson(github) }}"

